[project]
name = "zharness-next"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "langchain-anthropic>=1.7.0",
    "wcmatch>=11.0.1",
]

[tool.ruff]
extend-exclude = ["skills"]

[tool.uv.workspace]
members = [
    "zharness",
    "gateway",
]

"""ZHarness: a thread-scoped sandbox agent runtime library.

ZHarness 是一个线程级沙箱 agent 运行时库。

Provides the agent construction layer (`agents`), sandbox backends and lifecycle
management (`sandbox`), agent-facing tools (`tools`), a skill system (`skills`),
and the server-owned path resolution used to isolate each thread (`host`).

提供 agent 构建层（`agents`）、沙箱后端与生命周期管理（`sandbox`）、
面向 agent 的工具（`tools`）、技能系统（`skills`），以及用于隔离各线程的
服务器路径解析（`host`）。
"""
